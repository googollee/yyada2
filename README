mobile twitter反向代理，t.co反向代理

sub.domain.com是主站域名，sub2.domain.com是用于二次替换的辅助域名。因为sub_filter默认只能做一次替换，所以用两次反代来实现替换mobile.twitter.com和t.co。

sub3.domain.com是用于反代t.co的域名。

invite.txt里是允许登陆的白名单。每行一个小写的登录id或者email，注意是登录id，不是在twitter里@的那个id。如果没有这个文件，就不监测白名单。